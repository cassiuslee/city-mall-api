<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.citymall.api.module.goods.mapper.GoodsPriceRuleItemMapper">

    <select id="selectByRuleId" resultType="com.citymall.api.module.goods.entity.GoodsPriceRuleItem">
        select *
        from goods_price_rule_item
        where (f_delete_mark = 0 or f_delete_mark is null)
          and goods_rule_id = #{ruleId}
        order by f_creator_time asc
    </select>

    <delete id="deleteByRuleId">
        delete from goods_price_rule_item
        where goods_rule_id = #{ruleId}
    </delete>

    <insert id="batchInsert">
        insert into goods_price_rule_item (
        f_id,
        goods_rule_id,
        spu_id,
        goods_spec_id,
        sku_ids,
        market_types,
        sale_price,
        rule_dec,
        f_create_user_id,
        f_create_user_name,
        f_create_time,
        f_last_modify_user_id,
        f_last_modify_user_name,
        f_last_modify_time,
        f_delete_mark
        )
        values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.fId},
            #{item.goodsRuleId},
            #{item.spuId},
            #{item.goodsSpecId},
            #{item.skuIds},
            #{item.marketTypes},
            #{item.salePrice},
            #{item.ruleDec},
            #{item.fCreateUserId},
            #{item.fCreateUserName},
            #{item.fCreateTime},
            #{item.fLastModifyUserId},
            #{item.fLastModifyUserName},
            #{item.fLastModifyTime},
            #{item.fDeleteMark}
            )
        </foreach>
    </insert>

</mapper>