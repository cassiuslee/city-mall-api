<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.citymall.api.module.goods.mapper.GoodsPriceRuleItemMapper">

    <select id="selectByRuleId" resultType="com.citymall.api.module.goods.entity.GoodsPriceRuleItem">
        select *
        from goods_price_rule_item
        where (f_delete_mark = 0 or f_delete_mark is null)
          and goods_rule_id = #{ruleId}
        order by f_creator_time asc
    </select>

    <delete id="deleteByRuleId">
        delete
        from goods_price_rule_item
        where goods_rule_id = #{ruleId}
    </delete>

    <insert id="batchInsert">
        insert into goods_price_rule_item (
        f_id,
        goods_rule_id,
        spu_id,
        goods_spec_id,
        sku_ids,
        market_types,
        sale_price,
        rule_dec
        )
        values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.fId},
            #{item.goodsRuleId},
            #{item.spuId},
            #{item.goodsSpecId},
            #{item.skuIds},
            #{item.marketTypes},
            #{item.salePrice},
            #{item.ruleDec}
            )
        </foreach>
    </insert>

    <select id="selectByMarketType" resultType="com.citymall.api.module.goods.entity.GoodsPriceRuleItem">
        select *
        from goods_price_rule_item
        where (f_delete_mark = 0 or f_delete_mark is null)
          and market_types is not null
          and market_types != ''
          and find_in_set(#{marketType}, market_types)
        order by f_creator_time
    </select>

    <!--
        按 SPU_ID 查询规则项

        说明：
        1. 只取未删除数据
        2. 按创建时间排序（保证规则执行顺序稳定）
    -->
    <select id="selectBySpuId"
            resultType="com.citymall.api.module.goods.entity.GoodsPriceRuleItem">

        select *
        from goods_price_rule_item
        where (f_delete_mark = 0 or f_delete_mark is null)
          and spu_id = #{spuId}
        order by f_creator_time asc

    </select>

</mapper>