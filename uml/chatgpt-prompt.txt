接口：根据user_id查询用户及关联主体
方法：GET
路径：/customer/member-user-info
入参：userId
返回：用户 + 主体列表
类型：多表 join

请先生成DTO及VO我看下，带Knife4j注释

https://github.com/cassiuslee/city-mall-api.git 为项目后端
https://github.com/cassiuslee/city-mall-h5.git 为项止前端
你读取下最新代码，我们继续做项目前后端开发

https://github.com/cassiuslee/city-mall-api.git 为项目后端
https://github.com/cassiuslee/city-mall-h5.git 为项止前端
你读取下最新代码，其中/customer/member-user-info为查询用户信息接口，数据以些接口返回为准，你重新规划一下上述方案

重新按步聚列出完整可落地代码，带Knife4j注释

我现在需要改造前端my页面
用户有一个优雅的组件可切换经营主体，你先给出方案

https://github.com/cassiuslee/city-mall-api.git 为项目后端
https://github.com/cassiuslee/city-mall-h5.git 为项止前端
src/main/java/com/citymall/api/common/entity/BaseEntity.java 为基础实体
src/main/resources/db/goods.sql 为商品模块表
src/main/resources/db/customer.sql 为customer模块表
你读取分析下最新代码，再查找未能更新的问题

价格生成规则:
goods_spec_id 不为空
1.规格级，sku_ids 为空，marketType 为空，则生成对应所有spu_id,goods_spec_id的SKU价格
2.sku级，sku_ids 不为空，marketType 为空，则生成对应spu_id,goods_spec_id的指定sku_id,所有marketType的SKU价格
3.marketType级，sku_ids 为空，marketType 不为空，则生成对应spu_id,goods_spec_id的指定marketType,所有sku_id的SKU价格
4.sku与marketType交集sku_ids 不为空，marketType 不为空，则生成对应spu_id,goods_spec_id的指定marketType,指定sku_id的SKU价格
